name: frr
container:
  environment:
  - IPV4_NODE_NUMBER
  - NODE_IP
  - ASN_LOCAL
  entrypoint: /sbin/tini
  args:
  - --
  - /usr/lib/frr/docker-start
  security:
    writeableRootfs: true
    writeableSysfs: true
restart: always
