name: frr
container:
  depends:
    configuration: true
    network:
    - connectivity
  environment:
  - NODE_IP
  - ASN_LOCAL
  entrypoint: /sbin/tini
  args:
  - --
  - /usr/lib/frr/docker-start
  security:
    writeableRootfs: true
    writeableSysfs: true
restart: always
